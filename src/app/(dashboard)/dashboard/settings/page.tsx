import { SettingsManager } from "@/presentation/components/settings/settings-manager";

export default function SettingsPage() {
  return (
    <section>
      <SettingsManager />
    </section>
  );
}
